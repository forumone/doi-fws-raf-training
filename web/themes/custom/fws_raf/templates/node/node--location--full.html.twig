{#
/**
 * @file
 * Custom theme implementation to display a location node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
 * - content: A list of content items. Use 'content' to print all content, or
 *   content.field_name to access a specific field.
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - url: Direct URL of the current node.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 *
 * @see template_preprocess_node()
 */
#}

{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    'clearfix',
  ]
%}

{# Add Back Link #}
<div class="back-link" style="margin-bottom: 15px;">
  <a href="{{ base_path }}profile/locations-agents">
    <i class="fa fa-reply"></i> Back to Locations / Agents page
  </a>
</div>

<article{{ attributes.addClass(classes) }} style="margin-bottom: 22px;">
  <header>
    {{ title_prefix }}
    {% if not page %}
      <h2{{ title_attributes }}>
        <a href="{{ url }}" rel="bookmark">{{ label }}</a>
      </h2>
    {% endif %}
    {{ title_suffix }}
  </header>

  {# Location Information Group #}
  <div class="panel panel-primary">
    <div class="panel-heading">
      <h3 class="panel-title">Location Information</h3>
    </div>
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table table-bordered table-striped">
          <tbody>
            {% if content.field_location_address|render|trim %}
            <tr>
              <th class="col-md-3">Address</th>
              <td class="col-md-9">{{ content.field_location_address }}</td>
            </tr>
            {% endif %}

            {% if content.field_location_city|render|trim %}
            <tr>
              <th class="col-md-3">City</th>
              <td class="col-md-9">{{ content.field_location_city }}</td>
            </tr>
            {% endif %}

            {% if content.field_location_county|render|trim %}
            <tr>
              <th class="col-md-3">County</th>
              <td class="col-md-9">{{ content.field_location_county }}</td>
            </tr>
            {% endif %}

            {% if content.field_location_state_ref|render|trim %}
            <tr>
              <th class="col-md-3">State</th>
              <td class="col-md-9">{{ content.field_location_state_ref }}</td>
            </tr>
            {% endif %}

            {% if content.field_permit_no|render|trim %}
            <tr>
              <th class="col-md-3">Permit Number</th>
              <td class="col-md-9">{{ content.field_permit_no }}</td>
            </tr>
            {% endif %}
          </tbody>
        </table>
      </div>
    </div>
  </div>

  {# California Information Group #}
  {% if content.field_ca_county_select|render|trim or content.field_ca_access_key|render|trim %}
  <div class="panel panel-primary mt-3">
    <div class="panel-heading">
      <h3 class="panel-title">California Information</h3>
    </div>
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table table-bordered table-striped">
          <tbody>
            {% if content.field_ca_county_select|render|trim %}
            <tr>
              <th class="col-md-3">California County</th>
              <td class="col-md-9">{{ content.field_ca_county_select }}</td>
            </tr>
            {% endif %}

            {% if content.field_ca_access_key|render|trim %}
            <tr>
              <th class="col-md-3">Access Key</th>
              <td class="col-md-9">{{ content.field_ca_access_key }}</td>
            </tr>
            {% endif %}
          </tbody>
        </table>
      </div>
    </div>
  </div>
  {% endif %}

</article>
